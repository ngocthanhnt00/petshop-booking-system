export enum UserRoles {
  USER = 'user',
  ADMIN = 'admin',
  EMPLOYEE = 'employee'
}

export enum UserSex {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
  PREFER_NOT_TO_SAY = 'prefer not to say'
}

export enum UserStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive'
}
